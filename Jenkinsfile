pipeline {
    agent any

    stages {
        stage('Run Python Script') {
            steps {
                echo "Running new.py from the feature1 branch"
                sh 'python3 new.py' // Adjust the command if needed
            }
        }
    }
}
